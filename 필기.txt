Component: HTML을 return하는 function
  - <Component_name Property=value />
  - Lifecyle: Mount-Render-Update-Re_render-Update-....-Re_render-Unmount

React application은 하나의 Component만 Rendering해야 함

Component 내부의 Property를 얻는 방법
  1. function A(props){
      console.log(props.property_name);
  }
  2. function A({property_name})

map(function(current) {
})
map(current => {
})
위 두개는 같음.

React는 class Component의 render method를 모두 실행 함

setState를 사용하면 re-rendering 된다.